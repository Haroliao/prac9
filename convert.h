#ifndef CONVERT_H
#define CONVERT_H
#include <string>

class convert{
    public:
    //std::string converter(std::vector<std::string> x);
    double PolishPrefix(std::string prefix);
};
#endif